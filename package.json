{
  "name": "pmapp",
  "version": "0.0.0",
  "private": true,
  "description": "The Fay Preventive Maintenance Platform — internal CMMS monorepo",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "check": "pnpm -r typecheck && pnpm -r lint && pnpm -r build"
  }
}
